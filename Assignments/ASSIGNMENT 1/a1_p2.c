#include <stdio.h>

int main() {
    //question 2
    int result; /* The result of our calculation */
    result = (2 + 7) * 9 / 3;
    printf("The result is %d\n", result);
    /*the value of the integer(argument) was not specified in the 
    printf function, adding "",results" gives us the correct answer*/
    return 0;
}