#include <stdio.h>
    int main(){
    int i = 8;
    while (i >= 4){
         printf("i is %d\n", i);
    i--;
    }/*the syntax for the while loop to work must
    have curly brackets"{}" after the condition*/
    printf("That's it.\n");

return 0;
}

