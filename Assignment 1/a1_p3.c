#include <stdio.h>

int main(){
    /*
    CH-230-A
    a1 p3.[c or cpp or h]
    Jaydev Dhanji
    jdhanji@jacobs-university.de 
    */
    //question 3
    float result; /* The result of the division */
    int a = 5;
    int b = 13.5;
    result = a / b;
    printf("The result is %f\n", result);
    /*int values can not have decimal points if they do they will be counted as float numbers and not integers, also the
    formatting specifications for floating point is %f and not %d */

    return 0;
}