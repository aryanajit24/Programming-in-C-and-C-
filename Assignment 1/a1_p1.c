
#include <stdio.h>

int main() {
    double results;
    results = (double)(3+1)/5; /*the code did not work without the double because the answer was a fraction
    but if you add double before the equation it changes the fraction to decimal */
    printf ("the value of 4/5 is %1f \n", results);

return 0;
}
